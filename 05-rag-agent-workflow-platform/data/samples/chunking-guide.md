# Chunking guide

Chunking divides canonical text into independently retrievable fragments. Useful chunks preserve complete ideas while remaining small enough to rank precisely. Overlap can protect context near a boundary, but excessive overlap creates duplicates and weakens result diversity.

Each chunk needs stable provenance: its document version, ordered index, character offsets, and chunking version. These fields make citations reproducible after ingestion.

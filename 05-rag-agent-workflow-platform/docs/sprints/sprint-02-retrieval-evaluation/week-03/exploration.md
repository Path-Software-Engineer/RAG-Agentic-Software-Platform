# Week 3 Exploration — Public workflow and error analysis

The public workflow must enrich internal hit IDs with user-facing document metadata without altering rankings or metrics. Error analysis centers on incomplete recall rather than hiding low-performing queries. Manual labels are treated as reviewer evidence, not replacements for the original expected set.

The browser remains a presentation boundary. It never calculates similarity or metric values.

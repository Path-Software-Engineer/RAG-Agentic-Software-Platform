# Week 3 Review — API and Evaluation Lab

NestJS exposes strategies, test cases, runs, run detail, and relevance review under `/api/v1/evaluations`. SvelteKit provides setup controls, run history, metric cards, horizontal Recall@K comparison, an exact table, expandable error evidence, and accessible label actions. CORS includes PATCH and internal validation maps to a stable public 422 error.

Evidence: NestJS `evaluations` module and SvelteKit `/evaluation` route.

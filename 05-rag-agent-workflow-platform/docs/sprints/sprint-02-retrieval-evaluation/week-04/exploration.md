# Week 4 Exploration — Acceptance and limitations

Acceptance requires more than a compiled dashboard: an empty database must receive both migrations, canonical sources must support all strategies, public and internal resources must remain compatible, real HTTP calls must complete, and a manual label must be visible in the revised snapshot.

The reference corpus is intentionally small and synthetic. No fixed metric result is documented until the Docker-backed E2E runs in the user terminal.
